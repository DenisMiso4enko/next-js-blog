/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["cdn-icons-png.flaticon.com", "feature-sliced.design", "vgr.by" +
    ""],
  },
  // experimental: {
  //   serverActions: true,
  // },
};

module.exports = nextConfig;
